<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.su.supplydistributesystem.mapper.OrderItemMapper">

    <select id="selectById" resultType="com.su.supplydistributesystem.domain.OrderItem" parameterType="java.lang.Integer">
        select * from `order_item` where id= #{id} and `valid` = 1
    </select>

    <select id="select" resultType="com.su.supplydistributesystem.domain.OrderItem" parameterType="java.util.Map">
        select * from `order_item` where `valid` = 1
        <if test="id!=null">
            and `id` = #{id}
        </if>
    </select>

    <select id="selectList" resultType="com.su.supplydistributesystem.domain.OrderItem" parameterType="java.util.Map">
        select * from `order_item` where `valid` = 1
        <if test="id!=null">

        </if>
        <if test="startIndex != null and pageSize != null">
            limit #{startIndex},#{pageSize}
        </if>
    </select>

    <select id="selectCount" resultType="java.lang.Integer" parameterType="java.util.Map">
        select count(*) from `order_item` where `valid` = 1
        <if test="id!=null">

        </if>
    </select>

    <insert id="insert" parameterType="com.su.supplydistributesystem.domain.OrderItem" useGeneratedKeys="true" keyProperty="id">
        insert into `order_item`
        (
        `orderId`,
        `goodsId`,
        `goodSuppliyId`,
        `goodsName`,
        `taobaoPrice`,
        `jdPrice`,
        `price`,
        `supplyPrice`,
        `count`,
        `amount`,
        `profit1`,
        `profit2`,
        `profit3`,
        `createBy`,
        `updateBy`,
        `status`,
        )
        values
        (
        #{orderId},
        #{goodsId},
        #{goodSuppliyId},
        #{goodsName},
        #{taobaoPrice},
        #{jdPrice},
        #{price},
        #{supplyPrice},
        #{count},
        #{amount},
        #{profit1},
        #{profit2},
        #{profit3},
        #{createBy},
        #{createBy},
        #{status},
        )
    </insert>

    <update id="update" parameterType="com.su.supplydistributesystem.domain.OrderItem">
        update `order_item` set
        `orderId` = #{orderId},
        `goodsId` = #{goodsId},
        `goodSuppliyId` = #{goodSuppliyId},
        `goodsName` = #{goodsName},
        `taobaoPrice` = #{taobaoPrice},
        `jdPrice` = #{jdPrice},
        `price` = #{price},
        `supplyPrice` = #{supplyPrice},
        `count` = #{count},
        `amount` = #{amount},
        `profit1` = #{profit1},
        `profit2` = #{profit2},
        `profit3` = #{profit3},
        `updateBy` = #{updateBy},
        `status` = #{status},
        `updateTime` = now()
        where `id` = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        update `order_item` set valid = 0 where `id` = #{id}
    </delete>
</mapper>